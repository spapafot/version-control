<!-- Generated by scripts/gen-keyword-map.mjs. Edit src/challenges/seo.ts
     or src/lib/page-seo.ts and re-run `pnpm seo:keywords`. -->

# Keyword map

Every indexable URL on versioncontrol.gr (9 top-level pages plus 76 missions),
the query each one is written for, and the metadata it ships.

## Targeting

English, global. The `.gr` domain is not treated as a Greek-language signal: the course
is written in English, and Greek developers search these topics in English. There are no
`/el/` routes and no hreflang beyond `en`.

Head terms are saturated. Searches like "learn git" and "git tutorial" are held by
learngitbranching.js.org, boot.dev, Codecademy, gitbybit and git-practice.com, all with
far more authority than a new site. The strategy is therefore the long tail: one mission
page per specific problem a developer actually types into Google mid-panic, such as
"git reset vs revert", "fix detached HEAD" or "push rejected non-fast-forward".

## Top-level pages

These keep the site-name suffix from the layout title template, so their titles are
written short enough to leave room for it.

| URL | Primary keyword | Secondary | Title | Description |
| --- | --- | --- | --- | --- |
| `/` | learn git by doing | interactive git course; free git course; practice git in browser | Learn Git, using Git: a free interactive course | A free Git course you work through by running real commands in a browser terminal. 76 missions, a live commit graph, an optional free certificate. |
| `/stages/` | git learning path | git course curriculum; git topics for beginners; git syllabus | Git learning path: all 76 missions | The full course map, from your first commit through merge conflicts, the reflog and remotes. Pick the topic you need or work straight through in order. |
| `/playground/` | online git sandbox | practice git commands online; try git in browser; git simulator | Online Git sandbox: practice in-browser | An empty repository, a real terminal and a commit graph that redraws as you type. Try any Git command you like, with nothing at stake if it goes wrong. |
| `/quiz/` | git quiz | git interview questions; git multiple choice questions; test git knowledge | Git quiz: timed multiple choice | A timed Git quiz built from real scenarios, four answers each. Race the clock for three minutes or take a set of twenty, then compare on the leaderboard. |
| `/cheatsheet/` | git commands cheat sheet | git command list; git commands with examples; git reference | Git commands cheat sheet you can try | Every Git command worth knowing, what it does, and a link to the mission where you run it yourself. Written for people who keep forgetting the syntax. |
| `/account/` | free git certificate | git certification online; git course certificate; sync git progress | Your account and free Git certificate | Create a free account to sync your mission progress across devices and issue a shareable Git certificate with a permanent verification link. |
| `/verify/` | verify git certificate | certificate verification; open badges 3.0 verification; credential check | Verify a Git certificate | Check that a VersionControl.gr certificate is genuine. Enter a credential ID to see who earned it, when it was issued and the skills it covers. |
| `/privacy/` | privacy policy | cookie policy; analytics consent | Privacy & Cookies | What VersionControl.gr stores, what it never collects, and how analytics consent works. Accounts are optional, and nothing tracks you without consent. |
| `/terms/` | terms of use | terms and conditions | Terms of Use | The terms for using the free VersionControl.gr Git course, including what the in-browser Git engine is for and where it stops standing in for real Git. |

## Mission pages

76 long-tail pages, one per mission, and the main reason this site can rank at all.

Two things are deliberate here. Mission titles are **absolute**, with no site-name
suffix, so the whole SERP budget goes to the keyword. And the keyword title runs
*alongside* the on-screen title rather than replacing it: the course keeps its own voice
("The Referee", "! [rejected]", "The urgent phone call") while search engines get the
Git command. See `src/challenges/seo.ts`.


### World 1: The Terminal

The shell before the Git: look around, read and write files, and organise folders with paths.

| URL | Primary keyword | Title | Teaches |
| --- | --- | --- | --- |
| `/challenge/look-around/` | pwd and ls commands | pwd and ls: find where you are in the terminal | pwd; ls; the working directory |
| `/challenge/read-the-menu/` | cat command read file | cat: read a file without opening an editor | cat; reading files in the terminal |
| `/challenge/first-file/` | touch command create file | touch: create an empty file in the terminal | touch; creating files |
| `/challenge/write-it-down/` | echo write text to file | echo and the > redirect: write text into a file | echo; output redirection with > |
| `/challenge/append-a-line/` | append to file terminal | The >> redirect: append without overwriting | the >> redirect; > versus >> |
| `/challenge/make-a-folder/` | mkdir command | mkdir: make a folder and use paths to reach inside | mkdir; paths; ls with a folder argument |
| `/challenge/move-and-copy/` | mv and cp commands | mv and cp: move, rename and copy files | mv; cp; renaming files |
| `/challenge/spring-cleaning/` | rm remove file and directory | rm and rm -r: delete files and whole folders | rm; rm -r; deleting folders |

### World 2: First Steps

What Git is, what a repository is, and how to check its status.

| URL | Primary keyword | Title | Teaches |
| --- | --- | --- | --- |
| `/challenge/first-repository/` | git init | git init: turn a folder into a Git repository | git init; what a Git repository is |
| `/challenge/check-status/` | git status | git status: read what Git sees in your project | git status; tracked and untracked files |
| `/challenge/make-a-change/` | git status after editing a file | git status: see a file turn up as modified | git status; modified files; editing files in the terminal |
| `/challenge/spot-the-change/` | git status modified file | Find the modified file with git status, then stage it | git status; git add; modified files |
| `/challenge/two-kinds-of-change/` | modified vs untracked files git | Modified vs untracked files in Git | untracked files; modified files; git add |
| `/challenge/stage-everything/` | git add all files | git add: stage every change at once | git add .; staging multiple files |
| `/challenge/both-zones-at-once/` | file staged and modified git | When git status lists one file twice | staging area; git status; staged vs unstaged changes |

### World 3: Staging & Commits

Working tree, staging area, and your first commit.

| URL | Primary keyword | Title | Teaches |
| --- | --- | --- | --- |
| `/challenge/first-stage/` | git staging area explained | The Git staging area, explained by using it | staging area; git add |
| `/challenge/your-first-commit/` | git commit -m | git commit -m: write your first commit | git commit; writing commit messages |
| `/challenge/nothing-left-behind/` | git status after commit | git status after a commit: nothing left behind | git status; git commit; a clean working tree |
| `/challenge/see-the-difference/` | git diff --staged | git diff and --staged: check before you commit | git diff; git diff --staged |
| `/challenge/selective-commit/` | commit only one file git | Commit one file and leave the rest alone | selective staging; git add a single file |
| `/challenge/two-commits/` | split changes into separate commits | Split your work into two separate commits | one change per commit; git add; git commit |
| `/challenge/commit-shortcut/` | git commit -am | git commit -am: stage and commit in one step | git commit -am; tracked vs untracked files |
| `/challenge/explore-the-log/` | git log --oneline | git log and --oneline: read your history | git log; git log --oneline |

### World 4: Branches

Parallel lines of work: creating, switching, HEAD.

| URL | Primary keyword | Title | Teaches |
| --- | --- | --- | --- |
| `/challenge/first-branch/` | git branch create | git branch: create your first Git branch | git branch; what a branch is |
| `/challenge/switch-branch/` | git switch branch | git switch: move between Git branches | git switch; HEAD |
| `/challenge/commit-on-a-branch/` | git switch -c | git switch -c: branch, then commit on it | git switch -c; committing on a branch |
| `/challenge/back-to-main/` | git switch back to main | Switch back to main and watch your files change | git switch; one working tree per branch |
| `/challenge/where-am-i/` | which git branch am i on | Which branch am I on? Ask git branch | git branch; finding the current branch |
| `/challenge/branch-from-the-past/` | git branch from an earlier commit | Create a Git branch from an earlier commit | branching from a commit; git branch |
| `/challenge/the-old-way/` | git checkout vs git switch | git checkout vs git switch: the old way | git checkout; git checkout -b; checkout compared with switch |

### World 5: Merge

The full merge loop: branch, commit, switch, merge. Fast-forward and merge commits.

| URL | Primary keyword | Title | Teaches |
| --- | --- | --- | --- |
| `/challenge/first-merge/` | git merge branch into main | git merge: bring a branch back into main | git merge |
| `/challenge/fast-forward/` | git fast-forward merge | Fast-forward merge in Git, seen in the graph | fast-forward merge; git merge; the feature branch workflow |
| `/challenge/ship-the-specials/` | git feature branch merge practice | Create a branch, commit, and merge into main | git switch -c; git merge; the feature branch workflow |
| `/challenge/combine-branches/` | git merge commit diverged branches | Merge two diverged branches into one history | merge commit; diverged branches; the feature branch workflow |
| `/challenge/double-merge-day/` | fast-forward vs merge commit | Fast-forward or merge commit? Do both, compare | fast-forward merge; merge commit; the feature branch workflow |
| `/challenge/update-your-branch/` | merge main into feature branch | Merge main into your feature branch | merging main into a branch; keeping a branch up to date |
| `/challenge/catch-up-and-land/` | update feature branch then merge | Catch up from main, then merge your branch | merging main into a branch; landing a feature; git merge |
| `/challenge/clean-up-branches/` | git branch -d delete branch | git branch -d: delete a merged branch | git branch -d; deleting branches; the feature branch workflow |

### World 6: Merge Conflicts

Why they happen, how to read them, and how to resolve them.

| URL | Primary keyword | Title | Teaches |
| --- | --- | --- | --- |
| `/challenge/trigger-a-conflict/` | why git merge conflicts happen | Cause a Git merge conflict on purpose | merge conflicts; why conflicts happen |
| `/challenge/resolve-the-conflict/` | how to resolve a git merge conflict | How to resolve a Git merge conflict by hand | conflict markers; resolving a merge conflict; git add |
| `/challenge/finish-the-merge/` | complete git merge after conflict | Finish a Git merge after fixing the conflict | committing a conflict resolution; completing a merge |
| `/challenge/abort-the-merge/` | git merge --abort | git merge --abort: back out of a conflict | git merge --abort |
| `/challenge/conflict-solo/` | git merge conflict practice exercise | Resolve a merge conflict with no hints | merge conflicts; resolving a merge conflict |

### World 7: The Remote

A second copy of the repo lives on the café's server. fetch, pull, push: where your work meets Maria's.

| URL | Primary keyword | Title | Teaches |
| --- | --- | --- | --- |
| `/challenge/meet-the-remote/` | git remote -v origin | git remote -v: what origin actually is | git remote -v; origin; remote-tracking branches |
| `/challenge/fetch-the-news/` | git fetch vs pull | git fetch: download without changing your work | git fetch; origin/main |
| `/challenge/pull-it-in/` | git pull | git pull: bring the remote's work into yours | git pull; fetch plus merge |
| `/challenge/push-your-work/` | git push | git push: publish your commits to the server | git push; origin/main |
| `/challenge/the-rejected-push/` | git push rejected non-fast-forward | Push rejected? Fix non-fast-forward properly | rejected push; non-fast-forward; git pull before push |
| `/challenge/pull-with-conflict/` | git pull merge conflict | git pull with a merge conflict, start to end | git pull; merge conflicts; git push |
| `/challenge/closing-time/` | complete git workflow commit pull push | The full Git workflow: commit, pull, push, clean | git commit; git pull; git push; clean working tree |

### World 8: Fixing Mistakes

restore, reset, and revert: the right undo for every situation.

| URL | Primary keyword | Title | Teaches |
| --- | --- | --- | --- |
| `/challenge/restore-a-file/` | git restore discard changes | git restore: discard changes to a file | git restore; discarding uncommitted changes |
| `/challenge/unstage-a-file/` | git unstage a file | Unstage a file in Git without losing work | git restore --staged; unstaging a file |
| `/challenge/undo-a-local-commit/` | git undo last commit keep changes | Undo your last Git commit, keep the files | git reset; undoing a local commit |
| `/challenge/rewrite-the-message/` | git commit --amend message | git commit --amend: fix a commit message | git commit --amend; rewriting a commit message |
| `/challenge/scorched-earth/` | git reset --hard | git reset --hard: throw the experiment away | git reset --hard; discarding commits and changes |
| `/challenge/undo-a-published-commit/` | git revert pushed commit | git revert: safely undo a pushed commit | git revert; undoing a shared commit |
| `/challenge/choose-your-strategy/` | git reset vs revert vs restore | git reset vs revert vs restore: pick one | git reset; git revert; git restore |

### World 9: The Stash

Park unfinished work on a shelf, deal with the interruption, pick it back up.

| URL | Primary keyword | Title | Teaches |
| --- | --- | --- | --- |
| `/challenge/stash-and-switch/` | git stash | git stash: park unfinished work and switch | git stash; git stash pop |
| `/challenge/keep-a-copy/` | git stash apply vs pop | git stash apply vs pop: keep a copy | git stash apply; git stash pop |
| `/challenge/pick-the-right-stash/` | git stash list restore specific stash | git stash list: restore the right entry | git stash list; restoring a specific stash |
| `/challenge/clear-the-shelf/` | git stash drop clear | git stash drop and clear: empty the shelf | git stash drop; git stash clear |
| `/challenge/stash-to-the-right-branch/` | git stash to a new branch | Move stashed work onto the right branch | git stash; git switch -c; moving work between branches |

### World 10: Final Missions

Scenarios with no hand-holding. You pick the command.

| URL | Primary keyword | Title | Teaches |
| --- | --- | --- | --- |
| `/challenge/fix-a-bad-commit/` | fix wrong git commit title | Fix a wrong commit title, no hints given | git commit --amend |
| `/challenge/ship-the-feature/` | git merge then delete branch | Ship a feature branch, then delete it | git merge; git branch -d |
| `/challenge/save-the-merge/` | resolve conflict keep both changes | Resolve a conflict by keeping both changes | merge conflicts; keeping both sides of a conflict |
| `/challenge/clean-up-the-repo/` | git clean working tree | Get a Git repository back to a clean tree | git restore; reaching a clean working tree |
| `/challenge/grand-opening/` | git merge resolve conflict delete branch | Merge, resolve, delete: a full Git workflow | git merge; merge conflicts; git branch -d |
| `/challenge/mini-disaster/` | discard changes and revert a commit | Discard local changes and revert a commit | git restore; git revert |

### World 11: Git Disasters

Things going badly wrong, and how to get out: reflog, cherry-pick, detached HEAD.

| URL | Primary keyword | Title | Teaches |
| --- | --- | --- | --- |
| `/challenge/wrong-branch/` | committed on the wrong branch | Committed on the wrong branch? Move it | moving a commit to another branch; git reset; git branch |
| `/challenge/lost-commits/` | git reflog recover lost commits | git reflog: recover lost Git commits | git reflog; recovering lost commits |
| `/challenge/just-one-commit/` | git cherry-pick a single commit | git cherry-pick: take just one commit | git cherry-pick |
| `/challenge/detached-head/` | git detached HEAD fix | Detached HEAD in Git: what it is, how to leave | detached HEAD; git switch |
| `/challenge/buried-mistake/` | git revert an older commit | Revert an older commit deep in the history | git revert; git log |
| `/challenge/bad-rebase/` | undo a git rebase | Undo a bad Git rebase with the reflog | git reflog; git reset; undoing a rebase |
| `/challenge/rescue-the-branch/` | recover a deleted git branch | Recover a deleted Git branch | git reflog; git branch; recovering a deleted branch |
| `/challenge/conflict-hell/` | resolve two merge conflicts at once | Two conflicts in one merge: the final boss | merge conflicts; resolving several conflicts in one merge |

## Deliberate omissions

Each of these is a thing a checklist would tell you to add, left out on purpose:

- **`meta keywords`.** Google ignores it, and a stuffed list is a spam signal rather than a ranking factor.
- **`FAQPage` schema.** The site has no FAQ content. Marking up questions a visitor cannot see is what earns a manual action.
- **`SearchAction` in `WebSite`.** There is no site search to point it at.
- **`courseWorkload` in `CourseInstance`.** No completion time has been measured, and inventing one would put a false figure into structured data.
- **Per-mission OG images.** Social cards do not affect ranking, and 76 build-time images buy little over one good site-wide card.

## Known gap: apex vs www

`versioncontrol.gr` and `www.versioncontrol.gr` are both live custom domains in
`wrangler.jsonc`, which is duplicate content. Every canonical points at the apex, and
that is normally enough for Google to consolidate the two.

It is not fixed properly, though, and cannot be from inside this repo: a static export
emits no redirects, and Cloudflare's `_redirects` file matches paths only, not hostnames.
Closing it needs either a Redirect Rule in the Cloudflare dashboard or dropping the `www`
route from `wrangler.jsonc`.
